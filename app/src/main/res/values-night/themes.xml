<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FocusZone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/accent_dark</item>
        <item name="colorPrimaryVariant">@color/accent_dark_second</item>
        <item name="colorOnPrimary">@color/background_dark</item>
        <item name="colorSurface">@color/background_dark</item>
        <item name="colorOnSurface">@color/dark_text</item>
        <item name="android:navigationBarColor">@color/accent_dark</item>
    </style>
</resources>